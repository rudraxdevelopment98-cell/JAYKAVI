// Decorative gold top border shown on every page when the Traditional
// theme skin is active. Pure CSS (class defined in globals.css) — no JS.
export default function OrnateFrame() {
  return <div className="trad-frame-top" aria-hidden />;
}
